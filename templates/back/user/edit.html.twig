{% extends 'base.html.twig' %}

{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('css/admin.css') }}">
{% endblock %}
{% block title %}Modifier Utilisateur
{% endblock %}

{% block body %}
	{% include "fragments/_nav.html.twig" %}
	{% include "/fragments/_flash_message.html.twig" %}

	<div class="container user-edit-container">
		<div class="card">
			<div class="card-header text-center">
				<h2>📝 Modifier l'utilisateur</h2>
			</div>

			<div class="card-body">
				{{ include('back/user/_form.html.twig', {'button_label': 'Mettre à jour', 'button_class': 'btn-warning'}) }}
			</div>

			<div class="card-footer text-center">
				<a class="btn btn-secondary" href="{{ path('app_user_index') }}">⬅️ Retour</a>
				{{ include('back/user/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
